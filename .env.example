# Backend environment (optional overrides)
# Copy to backend/.env — no secrets required for the skeleton.

# Comma-separated browser origins for CORS (leave empty for sane localhost defaults).
CORS_ORIGINS=
